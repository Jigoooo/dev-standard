import { FlexColumn, Typography } from 'shared/ui';

export function ExcelUploadDownload() {
  return (
    <FlexColumn
      style={{
        height: '100%',
        gap: 16,
        paddingBlock: 32,
        paddingInline: 32,
        overflowY: 'auto',
      }}
    >
      <Typography></Typography>
    </FlexColumn>
  );
}
