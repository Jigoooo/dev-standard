import { FlexColumn } from '@/shared/components';

export function MyProfile() {
  return (
    <FlexColumn
      style={{
        height: '100%',
        gap: 16,
        paddingBlock: 32,
        paddingInline: 32,
        overflowY: 'auto',
      }}
    >
      <span></span>
    </FlexColumn>
  );
}
