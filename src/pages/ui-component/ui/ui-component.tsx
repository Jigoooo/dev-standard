export function UiComponent() {
  return (
    <div
      style={{
        height: '100%',
        maxHeight: 'calc(100vh - 200px)',
        maxWidth: 'calc(100vw - 300px)',
        overflow: 'hidden',
      }}
    ></div>
  );
}
